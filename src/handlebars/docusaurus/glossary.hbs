{{!-- Setup headers and necessary imports for root module --}}
{{#if root}}
---
title: Glossary
slug: {{slug}}/glossary
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Link from '@docusaurus/Link';

export const Highlight = ({ children, color }) => (
<span style=\{{ backgroundColor: color, borderRadius: '2px' , color: '#000' , padding: '0.2rem' }}>
    {children}
</span>
);
{{/if}}

## {{title}}

{{#each items as |item|}}
- <Link to="/docs{{../slug}}/{{../title}}#{{item.heading_id}}"> <Highlight color="{{item.color}}">{{item.type}}</Highlight> <code>{{{item.definition}}}</code></Link>
{{/each}}